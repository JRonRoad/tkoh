## Requirements

Python 3.8 or later with all `requirements.txt` dependencies installed, including `torch>=1.7`. To install run:
```bash
$ pip install -r requirements.txt
```

## Running Pipeline

Loading model > loading img > inference > results processing.
Please refer to the `main.py` file. Note that current model is just for api demo. It needs more training later to ensure the performance.